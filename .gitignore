# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/lib/generated

# env: .env.example is committed deliberately
!.env.example

# Generated knowledge graph (rebuild with /graphify)
graphify-out/

# Overture extracts — large, regenerable
data/

# local scratch (screenshots etc.)
tmp/

# The render source for public/car. Kept on disk, never committed: the
# frames are the artefact, but deleting the source once already cost a
# re-export.
Supercar_transforms_*.mp4

# Render sources for public/car. Kept on disk, never committed.
0831*.mov
