# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/lib/generated

# env: .env.example is committed deliberately
!.env.example

# Generated knowledge graph (rebuild with /graphify)
graphify-out/

# Overture extracts — large, regenerable
data/

# local scratch (screenshots etc.)
tmp/

# Reference renders. The car is generated in lib/landing/car-geometry.ts and
# ships no assets at all; these were only ever used to judge proportion and
# colour. Kept on disk, never committed.
Supercar_transforms_*.mp4

0831*.mov

# Source photographs for public/img. The derived WebP is committed; these
# masters are 9504px originals and stay on disk.
gt3rs.jpg
gtr.jpg
